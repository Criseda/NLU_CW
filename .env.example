# Hugging Face Token (Optional, not required)
# Used to bypass rate limits when downloading tokenizers and models.
# Get one at https://huggingface.co/settings/tokens, create a "Read" token
HF_TOKEN=hf_your_token_here